import React from 'react';
import SubTitle from "../../type/PageSubTitle";

const PodFooter = (props) => (
        <div className="Body">
            <SubTitle Title={props.Value} />
        </div>
    );
export default PodFooter;